import {
  useRequireRbacFeature
} from "./chunk-EOL4BCYA.js";
import {
  ListSummary
} from "./chunk-ZEUJHDIS.js";
import "./chunk-EGRHWZRV.js";
import {
  SectionRow
} from "./chunk-YAIOE67A.js";
import "./chunk-EGRHWZRV.js";
import {
  _DataTable,
  useDataTable
} from "./chunk-DTNZZN5Q.js";
import "./chunk-B45ZRF5P.js";
import "./chunk-UMNMC3WT.js";
import "./chunk-JEPTDSMQ.js";
import {
  SingleColumnPage
} from "./chunk-4EXUFKV5.js";
import {
  useQueryParams
} from "./chunk-OAFVYAKA.js";
import {
  useDate
} from "./chunk-HNYWGUEE.js";
import "./chunk-KV227VDS.js";
import {
  PermissionGuard
} from "./chunk-ZF4IASQG.js";
import {
  usePermissions
} from "./chunk-B5KOBJK3.js";
import "./chunk-UPGOECHU.js";
import {
  SingleColumnPageSkeleton
} from "./chunk-GRQOWLAF.js";
import "./chunk-M75V4PRK.js";
import "./chunk-MPXR7HT5.js";
import {
  rbacRolesQueryKeys,
  useDeleteRbacRole,
  useRbacAssignableRoles,
  useRbacRole,
  useRbacRoleUsers,
  useRemoveRbacRoleUsers
} from "./chunk-R43QHP6S.js";
import "./chunk-WCS3RWJR.js";
import {
  ActionMenu
} from "./chunk-ZHFL32RW.js";
import "./chunk-RPWFUYHH.js";
import "./chunk-YCBPSESL.js";
import "./chunk-YQRSOKFP.js";
import {
  queryClient
} from "./chunk-VMLNCWLE.js";
import "./chunk-CUPZIPFX.js";
import {
  sdk
} from "./chunk-56HJWUAY.js";
import {
  useExtension
} from "./chunk-7BUZUNHM.js";
import "./chunk-LK7TNEDZ.js";
import "./chunk-SCNTGT6L.js";
import "./chunk-PBGY4NPN.js";
import "./chunk-JH5JEPW4.js";
import {
  useTranslation
} from "./chunk-PELU447P.js";
import "./chunk-VR6VMGSS.js";
import "./chunk-R35JBZ3G.js";
import "./chunk-RSU5RGXO.js";
import {
  Button,
  Checkbox,
  Container,
  Heading,
  Key,
  PencilSquare,
  Trash,
  createColumnHelper,
  toast,
  usePrompt
} from "./chunk-XOMSU734.js";
import {
  require_jsx_runtime
} from "./chunk-KBTYAULA.js";
import "./chunk-5GLF3XJW.js";
import {
  Link,
  useLoaderData,
  useNavigate,
  useParams
} from "./chunk-QZIIENM5.js";
import "./chunk-RPCDYKBN.js";
import {
  require_react
} from "./chunk-QCHXOAYK.js";
import {
  __toESM
} from "./chunk-WOOG5QLI.js";

// node_modules/@medusajs/dashboard/dist/role-detail-QZS3AQLS.mjs
var import_jsx_runtime = __toESM(require_jsx_runtime(), 1);
var import_react = __toESM(require_react(), 1);
var import_jsx_runtime2 = __toESM(require_jsx_runtime(), 1);
var import_react2 = __toESM(require_react(), 1);
var import_jsx_runtime3 = __toESM(require_jsx_runtime(), 1);
var import_jsx_runtime4 = __toESM(require_jsx_runtime(), 1);
var ROLE_DETAIL_FIELDS = "id,name,description,created_at,updated_at,metadata,policies.id,policies.key,policies.resource,policies.operation,policies.description,users_link.user.id,users_link.user.first_name,users_link.user.last_name,users_link.user.email";
var RoleDetailBreadcrumb = (props) => {
  const { id } = props.params || {};
  const { role } = useRbacRole(
    id,
    {
      fields: ROLE_DETAIL_FIELDS
    },
    {
      initialData: props.data,
      enabled: Boolean(id)
    }
  );
  if (!role) {
    return null;
  }
  return (0, import_jsx_runtime.jsx)("span", { children: role.name });
};
var roleDetailQuery = (id) => ({
  queryKey: rbacRolesQueryKeys.detail(id, { fields: ROLE_DETAIL_FIELDS }),
  queryFn: async () => sdk.admin.rbacRole.retrieve(id, { fields: ROLE_DETAIL_FIELDS })
});
var roleLoader = async ({ params }) => {
  const id = params.id;
  const query = roleDetailQuery(id);
  return queryClient.ensureQueryData(query);
};
var RoleGeneralSection = ({ role }) => {
  const { t } = useTranslation();
  const { hasPermission } = usePermissions();
  const prompt = usePrompt();
  const navigate = useNavigate();
  const { mutateAsync: deleteRole, isPending: isDeleting } = useDeleteRbacRole(
    role.id
  );
  const handleDelete = async () => {
    const confirmed = await prompt({
      title: t("roles.delete.title"),
      description: t("roles.delete.description", { name: role.name }),
      confirmText: t("actions.delete"),
      cancelText: t("actions.cancel")
    });
    if (!confirmed) {
      return;
    }
    try {
      await deleteRole();
      toast.success(t("roles.delete.successToast", { name: role.name }));
      navigate("/settings/roles", { replace: true });
    } catch (error) {
      toast.error(error.message);
    }
  };
  const users = (0, import_react.useMemo)(() => {
    var _a;
    return ((_a = role.users_link) == null ? void 0 : _a.map((link) => link.user).filter((user) => !!user)) ?? [];
  }, [role.users_link]);
  const userLabels = (0, import_react.useMemo)(() => {
    return users.map((user) => {
      const fullName = `${user.first_name || ""} ${user.last_name || ""}`.trim();
      return fullName || user.email || user.id;
    });
  }, [users]);
  const permissionLabels = (0, import_react.useMemo)(() => {
    var _a;
    return ((_a = role.policies) == null ? void 0 : _a.map((policy) => {
      if (policy.key) {
        return policy.key;
      }
      const resource = policy.resource ?? "";
      const operation = policy.operation ?? "";
      if (resource && operation) {
        return `${resource}:${operation}`;
      }
      return policy.id;
    })) ?? [];
  }, [role.policies]);
  const canUpdate = hasPermission("rbac_role:update");
  const canDelete = hasPermission("rbac_role:delete");
  const groups = [];
  if (canUpdate) {
    groups.push({
      actions: [
        {
          icon: (0, import_jsx_runtime2.jsx)(PencilSquare, {}),
          label: t("actions.edit"),
          to: `edit`
        },
        {
          icon: (0, import_jsx_runtime2.jsx)(Key, {}),
          label: t("roles.actions.managePermissions"),
          to: `permissions`
        }
      ]
    });
  }
  if (canDelete) {
    groups.push({
      actions: [
        {
          icon: (0, import_jsx_runtime2.jsx)(Trash, {}),
          label: t("actions.delete"),
          onClick: handleDelete,
          disabled: isDeleting
        }
      ]
    });
  }
  return (0, import_jsx_runtime2.jsxs)(Container, { className: "divide-y p-0", children: [
    (0, import_jsx_runtime2.jsxs)("div", { className: "flex items-center justify-between px-6 py-4", children: [
      (0, import_jsx_runtime2.jsx)(Heading, { children: role.name }),
      groups.length && (0, import_jsx_runtime2.jsx)(ActionMenu, { groups })
    ] }),
    (0, import_jsx_runtime2.jsx)(SectionRow, { title: t("fields.description"), value: role.description }),
    hasPermission("user:read") && (0, import_jsx_runtime2.jsx)(
      SectionRow,
      {
        title: t("users.domain"),
        value: userLabels.length ? (0, import_jsx_runtime2.jsx)("div", { className: "inline-flex", children: (0, import_jsx_runtime2.jsx)(ListSummary, { list: userLabels }) }) : "-"
      }
    ),
    hasPermission("rbac_policy:read") && (0, import_jsx_runtime2.jsx)(
      SectionRow,
      {
        title: t("roles.fields.permissions"),
        value: permissionLabels.length ? (0, import_jsx_runtime2.jsx)("div", { className: "inline-flex", children: (0, import_jsx_runtime2.jsx)(ListSummary, { list: permissionLabels }) }) : "-"
      }
    )
  ] });
};
var PAGE_SIZE = 10;
var RoleUsersSection = ({ role }) => {
  const [rowSelection, setRowSelection] = (0, import_react2.useState)({});
  const { t } = useTranslation();
  const prompt = usePrompt();
  const { offset, order } = useQueryParams(["offset", "order"]);
  const { hasAllPermissions } = usePermissions();
  const canManageRole = hasAllPermissions(["user:update", "rbac_role:update"]);
  const { data: assignableData } = useRbacAssignableRoles(void 0, {
    enabled: canManageRole
  });
  const isRoleAssignable = ((assignableData == null ? void 0 : assignableData.roles) ?? []).some(
    (r) => r.id === role.id
  );
  (0, import_react2.useEffect)(() => {
    if (!isRoleAssignable && Object.keys(rowSelection).length) {
      setRowSelection({});
    }
  }, [isRoleAssignable, rowSelection, setRowSelection]);
  const {
    users,
    count,
    isPending: isLoading,
    isError,
    error
  } = useRbacRoleUsers(role.id, {
    limit: PAGE_SIZE,
    offset: offset ? parseInt(offset) : 0,
    order
  });
  const columns = useColumns(isRoleAssignable);
  const { table } = useDataTable({
    data: users ?? [],
    columns,
    count,
    getRowId: (row) => row.id,
    enablePagination: true,
    enableRowSelection: isRoleAssignable,
    pageSize: PAGE_SIZE,
    rowSelection: {
      state: rowSelection,
      updater: setRowSelection
    },
    meta: {
      roleId: role.id,
      isRoleAssignable
    }
  });
  if (isError) {
    throw error;
  }
  const { mutateAsync } = useRemoveRbacRoleUsers(role.id);
  const handleRemove = async () => {
    if (!isRoleAssignable) {
      return;
    }
    const keys = Object.keys(rowSelection);
    if (!keys.length) {
      return;
    }
    const res = await prompt({
      title: t("roles.users.remove.title", {
        count: keys.length
      }),
      description: t("roles.users.remove.description", {
        count: keys.length
      }),
      confirmText: t("actions.continue"),
      cancelText: t("actions.cancel")
    });
    if (!res) {
      return;
    }
    await mutateAsync(keys, {
      onSuccess: () => {
        setRowSelection({});
      }
    });
  };
  return (0, import_jsx_runtime3.jsxs)(Container, { className: "divide-y p-0", children: [
    (0, import_jsx_runtime3.jsxs)("div", { className: "flex items-center justify-between px-6 py-4", children: [
      (0, import_jsx_runtime3.jsx)(Heading, { level: "h2", children: t("users.domain") }),
      isRoleAssignable && (0, import_jsx_runtime3.jsx)(Link, { to: "add-users", children: (0, import_jsx_runtime3.jsx)(Button, { variant: "secondary", size: "small", children: t("general.add") }) })
    ] }),
    (0, import_jsx_runtime3.jsx)(
      _DataTable,
      {
        table,
        columns,
        pageSize: PAGE_SIZE,
        isLoading,
        count,
        navigateTo: (row) => `/settings/users/${row.original.id}`,
        orderBy: [
          { key: "email", label: t("fields.email") },
          { key: "first_name", label: t("fields.firstName") },
          { key: "last_name", label: t("fields.lastName") },
          { key: "created_at", label: t("fields.createdAt") },
          { key: "updated_at", label: t("fields.updatedAt") }
        ],
        commands: isRoleAssignable ? [
          {
            action: handleRemove,
            label: t("actions.remove"),
            shortcut: "r"
          }
        ] : void 0,
        noRecords: {
          message: t("roles.users.list.noRecordsMessage")
        },
        pagination: true
      }
    )
  ] });
};
var RoleUserActions = ({
  user,
  roleId,
  isRoleAssignable
}) => {
  const { t } = useTranslation();
  const prompt = usePrompt();
  const { mutateAsync } = useRemoveRbacRoleUsers(roleId);
  if (!isRoleAssignable) {
    return null;
  }
  const handleRemove = async () => {
    const res = await prompt({
      title: t("roles.users.remove.title", {
        count: 1
      }),
      description: t("roles.users.remove.description", {
        count: 1
      }),
      confirmText: t("actions.continue"),
      cancelText: t("actions.cancel")
    });
    if (!res) {
      return;
    }
    await mutateAsync([user.id]);
  };
  return (0, import_jsx_runtime3.jsx)(
    ActionMenu,
    {
      groups: [
        {
          actions: [
            {
              icon: (0, import_jsx_runtime3.jsx)(Trash, {}),
              label: t("actions.remove"),
              onClick: handleRemove
            }
          ]
        }
      ]
    }
  );
};
var columnHelper = createColumnHelper();
var useColumns = (isRoleAssignable) => {
  const { t } = useTranslation();
  const { getFullDate } = useDate();
  return (0, import_react2.useMemo)(
    () => [
      columnHelper.display({
        id: "select",
        header: ({ table }) => {
          return (0, import_jsx_runtime3.jsx)(
            Checkbox,
            {
              checked: table.getIsSomePageRowsSelected() ? "indeterminate" : table.getIsAllPageRowsSelected(),
              disabled: !isRoleAssignable,
              onCheckedChange: (value) => table.toggleAllPageRowsSelected(!!value)
            }
          );
        },
        cell: ({ row }) => {
          return (0, import_jsx_runtime3.jsx)(
            Checkbox,
            {
              checked: row.getIsSelected(),
              disabled: !row.getCanSelect(),
              onCheckedChange: (value) => row.toggleSelected(!!value),
              onClick: (e) => {
                e.stopPropagation();
              }
            }
          );
        }
      }),
      columnHelper.accessor("first_name", {
        header: t("fields.firstName"),
        cell: ({ row }) => row.original.first_name || "-"
      }),
      columnHelper.accessor("last_name", {
        header: t("fields.lastName"),
        cell: ({ row }) => row.original.last_name || "-"
      }),
      columnHelper.accessor("email", {
        header: t("fields.email"),
        cell: ({ row }) => row.original.email
      }),
      columnHelper.accessor("created_at", {
        header: t("fields.createdAt"),
        cell: ({ row }) => getFullDate({ date: row.original.created_at })
      }),
      columnHelper.display({
        id: "actions",
        cell: ({ row, table }) => {
          const { roleId, isRoleAssignable: rowisRoleAssignable } = table.options.meta;
          return (0, import_jsx_runtime3.jsx)(
            RoleUserActions,
            {
              user: row.original,
              roleId,
              isRoleAssignable: rowisRoleAssignable
            }
          );
        }
      })
    ],
    [t, getFullDate, isRoleAssignable]
  );
};
var RoleDetail = () => {
  const initialData = useLoaderData();
  const { id } = useParams();
  const { getWidgets } = useExtension();
  const isRbacEnabled = useRequireRbacFeature();
  const {
    role,
    isPending: isLoading,
    isError,
    error
  } = useRbacRole(
    id,
    { fields: ROLE_DETAIL_FIELDS },
    {
      initialData,
      enabled: !!id && isRbacEnabled
    }
  );
  if (!isRbacEnabled) {
    return null;
  }
  if (isError) {
    throw error;
  }
  if (isLoading || !role) {
    return (0, import_jsx_runtime4.jsx)(SingleColumnPageSkeleton, { sections: 2, showJSON: true, showMetadata: true });
  }
  return (0, import_jsx_runtime4.jsxs)(
    SingleColumnPage,
    {
      data: role,
      showJSON: true,
      showMetadata: true,
      widgets: {
        before: getWidgets("role.details.before"),
        after: getWidgets("role.details.after")
      },
      children: [
        (0, import_jsx_runtime4.jsx)(RoleGeneralSection, { role }),
        (0, import_jsx_runtime4.jsx)(PermissionGuard, { permission: "user:read", children: (0, import_jsx_runtime4.jsx)(RoleUsersSection, { role }) })
      ]
    }
  );
};
export {
  RoleDetailBreadcrumb as Breadcrumb,
  RoleDetail as Component,
  roleLoader as loader
};
//# sourceMappingURL=role-detail-QZS3AQLS-W6PELCPP.js.map
