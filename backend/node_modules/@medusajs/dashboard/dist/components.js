"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/exports/components.ts
var components_exports = {};
__export(components_exports, {
  LayoutComposer: () => LayoutComposer
});
module.exports = __toCommonJS(components_exports);

// src/components/layout-composer/layout-composer.tsx
var import_react_router_dom = require("react-router-dom");

// src/providers/extension-provider/use-extension.tsx
var import_react2 = require("react");

// src/providers/extension-provider/extension-context.tsx
var import_react = require("react");
var ExtensionContext = (0, import_react.createContext)(null);

// src/providers/extension-provider/use-extension.tsx
var useExtension = () => {
  const context = (0, import_react2.useContext)(ExtensionContext);
  if (!context) {
    throw new Error("useExtension must be used within a ExtensionProvider");
  }
  return context;
};

// src/components/layout-composer/constants.ts
var CORE_CONTENT_ORDER = 100;

// src/components/layout-composer/layout-composer.tsx
var import_jsx_runtime = require("react/jsx-runtime");
var LayoutComposer = ({
  widgetsZonePrefix,
  preferredLayoutId,
  sections,
  data,
  hasOutlet = true
}) => {
  const { getWidgetsForSections, getLayout } = useExtension();
  const layoutId = preferredLayoutId;
  const layout = getLayout(layoutId);
  const widgetsBySection = getWidgetsForSections(
    widgetsZonePrefix,
    layout?.sections?.map((s) => s.id) ?? []
  );
  const widgetProps = { data };
  const renderedSections = {};
  for (const { id } of layout?.sections ?? []) {
    const widgets = widgetsBySection[id] ?? [];
    const before = widgets.filter((w) => w.order < CORE_CONTENT_ORDER);
    const after = widgets.filter((w) => w.order >= CORE_CONTENT_ORDER);
    renderedSections[id] = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
      before.map(({ Component }, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Component, { ...widgetProps }, i)),
      sections[id],
      after.map(({ Component }, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Component, { ...widgetProps }, i))
    ] });
  }
  const LayoutComponent = layout?.Component;
  if (!LayoutComponent) return null;
  return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LayoutComponent, { sections: renderedSections, data }),
    hasOutlet && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_react_router_dom.Outlet, {})
  ] });
};
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  LayoutComposer
});
